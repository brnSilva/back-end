spring:
  application:
    name: challenge

  h2:
    console:
      enabled: true

  datasource:
    url: jdbc:h2:mem:challenge
    driverClassName: org.h2.Driver
    username: sa
    password:
  
  jpa:
    hibernate:
      ddl-auto: update

server:
  servlet:
    context-path: /api/v1